let initialState = {
    movies: []
}

function MovieReducer(state= initialState, action) {
    switch(action.type){
        default:{
            return state
        }
    }
}

export default MovieReducer